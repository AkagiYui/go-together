# 示例环境变量文件（开发环境可复制为 .env 使用）
# PostgreSQL 连接串（必须）
DSN=postgres://postgres:postgres@localhost:5432/nottodo?sslmode=disable

# 运行模式：prod 或 dev（可选，默认 prod）
MODE=dev

# 监听地址（可选，默认 0.0.0.0）
HOST=0.0.0.0

# 监听端口（可选，默认 8082）
PORT=8082
