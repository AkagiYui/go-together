# HTTP 服务监听端口
PORT=8080

# GitHub webhook secret（必需）
# 在 GitHub 仓库设置中配置 webhook 时设置的 secret
WEBHOOK_SECRET=your_webhook_secret_here

# 要匹配的仓库名称（必需）
# 格式: owner/repo
REPOSITORY_NAME=akagiyui/go-together

# 要匹配的分支名称（必需）
BRANCH_NAME=master

# 要匹配的工作流文件名（必需）
# 可以是完整路径或文件名，程序会检查路径是否以此结尾
WORKFLOW_FILE_NAME=deploy.yml

# Docker Compose 文件路径
# 相对于 COMPOSE_PROJECT_DIR 的路径
COMPOSE_FILE_PATH=docker-compose.yml

# Docker Compose 项目目录
# docker compose 命令的工作目录
COMPOSE_PROJECT_DIR=/path/to/your/project

