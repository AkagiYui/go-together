name: Build Keep Traffic Ratio

on:
  workflow_dispatch:
  push:
    branches:
      - master
    paths:
      - 'keep-traffic-ratio/**'
      - '.github/workflows/keep-traffic-ratio.yml'
    tags:
      - 'keep-traffic-ratio-v*'
  pull_request:
    branches:
      - master
    paths:
      - 'keep-traffic-ratio/**'

jobs:
  build:
    name: Build Multi-Platform Binaries
    runs-on: ubuntu-latest
    permissions:
      contents: read
    
    strategy:
      matrix:
        include:
          # Linux
          - goos: linux
            goarch: amd64
            output: keep-traffic-ratio-linux-amd64
          - goos: linux
            goarch: arm64
            output: keep-traffic-ratio-linux-arm64
          - goos: linux
            goarch: arm
            goarm: 7
            output: keep-traffic-ratio-linux-armv7
          - goos: linux
            goarch: arm
            goarm: 6
            output: keep-traffic-ratio-linux-armv6
          
          # Windows
          - goos: windows
            goarch: amd64
            output: keep-traffic-ratio-windows-amd64.exe
          - goos: windows
            goarch: arm64
            output: keep-traffic-ratio-windows-arm64.exe
          
          # macOS
          - goos: darwin
            goarch: amd64
            output: keep-traffic-ratio-darwin-amd64
          - goos: darwin
            goarch: arm64
            output: keep-traffic-ratio-darwin-arm64
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
      
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: 'keep-traffic-ratio/go.mod'
      
      - name: Build binary for ${{ matrix.goos }}/${{ matrix.goarch }}
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          GOARM: ${{ matrix.goarm }}
        run: |
          cd keep-traffic-ratio
          go build -v -trimpath -ldflags="-s -w" -o ${{ matrix.output }} .
          ls -lh ${{ matrix.output }}
      
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.output }}
          path: keep-traffic-ratio/${{ matrix.output }}
          retention-days: 30
  
  # 创建 release 时自动上传所有二进制文件
  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    if: startsWith(github.ref, 'refs/tags/keep-traffic-ratio-v')
    
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
      
      - name: Display structure of downloaded files
        run: ls -R artifacts
      
      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/**/*
          draft: false
          prerelease: false
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

