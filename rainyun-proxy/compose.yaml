services:
  application:
    build:
      context: .
      dockerfile: Containerfile
    restart: always
    ports:
      - "28183:28183"
    healthcheck:
      test: ["CMD", "/workspace/app", "-healthcheck"]
      interval: 60s
      retries: 5
      start_period: 20s
      timeout: 10s